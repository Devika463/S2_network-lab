
echo "Enter the elements of the array sepaarted by spaces:"
read -a array
echo "The elements in the array are:"
for element in "${array[@]}"
do
        echo $element
done

  output
  -------

  [mca@localhost ~]$ vi elements.sh
[mca@localhost ~]$ chmod +x elements.sh
[mca@localhost ~]$ ./ elements.sh
bash: ./: Is a directory
[mca@localhost ~]$ ./elements.sh
Enter the elements of the array sepaarted by spaces:
1 2 hello 9
The elements in the array are:
1
2
hello
9
